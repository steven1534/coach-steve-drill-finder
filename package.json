{
  "name": "coach-steve-drill-finder-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node scripts/test-sync.mjs",
    "build": "node scripts/sync-build.mjs"
  },
  "engines": {
    "node": "24.x"
  }
}
